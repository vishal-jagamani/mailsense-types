// Sorting order for email search & list queries
export enum EmailSearchSortOrder {
    ASC = 'asc',
    DESC = 'desc',
}

export enum EMAIL_STATUS {
    RECEIVED = 'received',
    DRAFT = 'draft',
    SENT = 'sent',
}
